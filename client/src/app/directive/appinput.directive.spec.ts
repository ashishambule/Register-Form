import { AppinputDirective } from './appinput.directive';

describe('AppinputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppinputDirective();
    expect(directive).toBeTruthy();
  });
});
